name: Test
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test ssh
        run: |
          echo ${{ secrets.DEPLOY_KEY }} |  ssh -i /dev/stdin ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }} "whoami && sudo whoami"